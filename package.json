{
  "name": "kus-bahcesi",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:android-web": "CAPACITOR_BUILD=1 VITE_NATIVE_BUILD=true vite build",
    "android:add": "npm run build:android-web && cap add android",
    "android:sync": "npm run build:android-web && cap sync android",
    "android:open": "npm run android:sync && cap open android",
    "android:debug": "npm run android:sync && cd android && ./gradlew assembleDebug",
    "android:bundle": "npm run android:sync && cd android && ./gradlew bundleRelease",
    "preview": "vite preview --host 0.0.0.0",
    "check": "for file in src/*.js; do node --check \"$file\" || exit 1; done",
    "test": "npm run build --silent && node --test"
  },
  "devDependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "happy-dom": "^20.12.0",
    "vite": "^7.1.3"
  },
  "dependencies": {
    "@capacitor-community/admob": "^8.1.0",
    "@capacitor-community/keep-awake": "^8.0.1",
    "@capacitor/app": "^8.1.1",
    "@capacitor/browser": "^8.0.4",
    "@capacitor/core": "^8.5.1"
  }
}
