{
  "name": "kus-bahcesi",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "check": "node --check src/main.js && node --check src/game-core.js",
    "test": "npm run build --silent && node --test"
  },
  "devDependencies": {
    "happy-dom": "^20.12.0",
    "vite": "^7.1.3"
  }
}
